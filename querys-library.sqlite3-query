-- database: ./database.db

SELECT * FROM books;

-- query to select every item from books (film, games and vynils) tabel --
SELECT book_id, book_name, image_url FROM books;
-- add future tables later


-- query to select login information --
SELECT users.user_id, user_name, password
FROM users;

-- query to input signuo info --
INSERT INTO users (email, username, password, first_name, last_name) 
VALUES (?, ?, ?, ?, ?);

-- query to change username --
UPDATE users
SET user_name = 'bbbbbbbbbb'
WHERE user_id = 16;

-- delete user information --
CREATE TRIGGER delete_user_data
BEFORE DELETE ON users
BEGIN
    DELETE FROM users_books WHERE user_id = OLD.id;
    DELETE FROM wishlist WHERE user_id = OLD.id;
END;

DELETE FROM users WHERE user_id = 15;

DROP TRIGGER delete_user_data